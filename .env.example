# Snowflake Configuration
# Replace these with your actual Snowflake credentials

# Account Identifier - Find this in your Snowflake web UI
# Modern format (post-Nov 2020): orgname-accountname
# Legacy format: account_locator.region.cloud (e.g., xy12345.us-east-1.aws)
# Look at your Snowflake URL to find it: https://<account_identifier>.snowflakecomputing.com
SNOWFLAKE_ACCOUNT=your_account_identifier

# Your Snowflake username
SNOWFLAKE_USER=your_username

# Your Snowflake password
SNOWFLAKE_PASSWORD=your_password

# Role with necessary privileges (e.g., ACCOUNTADMIN, SYSADMIN, or custom role)
SNOWFLAKE_ROLE=your_role

# Database name (will be created by data_ingestion.py if it doesn't exist)
SNOWFLAKE_DATABASE=DEMO_DB

# Warehouse name (must already exist in your Snowflake account)
SNOWFLAKE_WAREHOUSE=your_warehouse

# Schema name for development environment
SNOWFLAKE_SCHEMA=DEV
